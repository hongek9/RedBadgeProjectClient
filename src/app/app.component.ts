import { Component } from '@angular/core';

// sessionToken: string;
// const sessionToken = localStorage.getItem('sessionToken');

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})



export class AppComponent {
  title = 'cup-o-joy-client';
}
